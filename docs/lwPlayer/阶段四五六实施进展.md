# 阶段四、五、六实施进展

## 概述

根据 `docs/lwPlayer/阶段四五六实施计划.md`，记录各阶段的实施进展。

## 阶段四：统计信息系统（优先级：中）

### 状态：未开始

### 计划任务
- [ ] 创建 `Statistics` 类
- [ ] 集成到 `VideoThread`、`AudioThread`、`PlayController`、`VideoRenderer`
- [ ] 在 UI 中显示统计信息（可选）

### 涉及文件
- `videoDecoder/Statistics.h/cpp` (待创建)
- `videoDecoder/VideoThread.cpp` (待修改)
- `videoDecoder/AudioThread.cpp` (待修改)
- `PlayController.cpp` (待修改)
- `videoDecoder/opengl/OpenGLRenderer.cpp` (待修改)

---

## 阶段五：字幕系统集成（优先级：中）

### 状态：部分完成

### 当前状态
- ✅ `SubtitleWidget` 已存在并集成到 `StereoVideoWidget`
- ✅ 字幕加载、显示、更新功能已实现
- ⏸️ 字幕渲染未集成到视频渲染管线（仍使用独立的 `SubtitleWidget`）

### 计划任务
- [ ] 创建 `SubtitleRenderer` 类（集成到渲染管线）
- [ ] 集成到 `VideoRenderer` 渲染管线
- [ ] 支持 3D 模式下的字幕渲染
- [ ] 使用 `AVClock` 同步字幕时间

### 涉及文件
- `videoDecoder/subtitle/SubtitleRenderer.h/cpp` (待创建)
- `videoDecoder/subtitle/SubtitleDecoder.h/cpp` (待创建)
- `videoDecoder/opengl/OpenGLRenderer.cpp` (待修改)
- `videoDecoder/opengl/StereoOpenGLRenderer.cpp` (待修改)

---

## 阶段六：播放列表与播放控制优化（优先级：低，UI重构）

### 状态：未开始

### 计划任务
- [ ] 创建独立的 `Playlist` 类
- [ ] 创建 `PlaylistItem` 类
- [ ] 集成到 `PlayController`
- [ ] UI 重构（播放列表、播放控制按钮独立化）

### 涉及文件
- `playlist/Playlist.h/cpp` (待创建)
- `playlist/PlaylistItem.h/cpp` (待创建)
- `PlayController.h/cpp` (待修改)
- `MainWindow.h/cpp` (待修改，UI重构)

---

## 实施优先级调整

### 当前重点任务（按优先级）

**P0 - 最高优先级（核心功能修复）**：

1. **硬件解码修复**
   - ✅ 已修复 transferFrame 中的 sw_frame 初始化问题
   - ✅ 已启用硬件解码
   - ⚠️ **需要测试验证是否解决黑画面问题**

2. **基础播放功能修复**
   - ⏸️ 色彩问题（可能与 shader 相关）
   - ⏸️ 无声音
   - ⏸️ Seeking 后画面/音频不同步
   - ⏸️ AV 同步问题

3. **Shader 还原**
   - ⏸️ 使用 branches 中的 fragment.glsl 替换当前 shader

**P1 - 高优先级（重要功能）**：

4. **测试闭环完善**
   - ✅ 已创建进度条和 Seeking 测试
   - ✅ 已创建音频测试
   - ⚠️ **需要运行测试验证效果**

**P2 - 中优先级（功能增强）**：

5. **阶段四、六实施**
   - ⏸️ 阶段四：统计信息系统
   - ⏸️ 阶段六：播放列表优化
   - ⏸️ 阶段五：字幕系统集成（部分完成，字幕渲染未集成到渲染管线）

**P3 - 低优先级（架构优化，支线任务）**：

6. **Camera 功能迁移** ✅（已完成）
   - ✅ 创建了 `CameraManager` 类
   - ✅ 在 `MainWindow` 中集成
   - ✅ Camera Widget 同级化（CameraOpenGLWidget 现在与 StereoVideoWidget 同级）
   - ✅ 移除 `StereoVideoWidget` 中的 Camera 接口（已移除 cameraOpenGLWidget_ 成员）
   - ✅ 在 `MainWindow` 中实现 `switchToVideoFile()` 和 `switchToCamera()` 方法
   - ✅ 在 `openPath()` 中添加切换到视频文件的逻辑

**注意**：Camera 重构已完成。虽然这是支线任务，但已按架构改进方案完成。接下来应聚焦核心功能修复（硬件解码、基础播放功能）和阶段四、六实施。

---

## 下一步行动

### 立即执行
1. ✅ 完成 Camera 功能迁移（已完成）
2. ⏸️ 测试硬件解码修复效果
3. ⏸️ 还原 Shader 文件
4. ⏸️ 运行新的测试模块（进度条、音频）

### 短期（1-2周）
1. 修复硬件解码问题（如果仍有问题）
2. 完善测试闭环
3. 修复色彩、音频、Seeking 等问题

### 中期（2-4周）
1. 实施阶段四（统计信息系统）
2. 实施阶段五（字幕系统集成到渲染管线）
3. 继续 BUG 修复和优化

### 长期（1-2月）
1. 实施阶段六（播放列表优化和 UI 重构）
2. 性能优化
3. 稳定性测试
