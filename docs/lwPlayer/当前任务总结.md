# 当前任务总结

## 用户需求

1. **硬件解码问题**：参考QtAV重构hardware_decoder和FFDecHW
2. **颜色问题**：还原之前验证过的shader（branches中的fragment.glsl和vertex.glsl）
3. **UI布局问题**：按照图片调整UI布局（视频在左/中央，播放列表在右侧，控制条在底部）
4. **调试支持**：集成DebugImageSaver，在TRACE模式下保存图像

## 已完成

### 1. 硬件解码修复（部分）
- ✅ 修复了transferFrame中sw_frame初始化问题
- ✅ 启用了硬件解码
- ⚠️ 需要进一步参考QtAV重构

### 2. 测试闭环完善
- ✅ 创建了进度条和Seeking测试
- ✅ 创建了音频测试
- ✅ 更新了完整测试套件

## 待完成

### 1. 硬件解码重构（高优先级）
**参考QtAV的实现**：
- 改进get_format回调（参考QtAV的getFormat）
- 优化transferFrame实现
- 确保正确初始化sw_frame

**文件**：
- `videoDecoder/hardware_decoder.cc`
- `videoDecoder/hardware_decoder.h`
- `videoDecoder/FFDecHW.cpp`

### 2. Shader还原（高优先级）
**问题**：当前shader使用了复杂的YUV转换，branches中的shader使用简单公式

**操作**：
- 使用`branches/WZMediaPlay/Shader/fragment.glsl`替换`videoDecoder/opengl/shaders/StereoVideoYCbCr.frag`
- 使用`branches/WZMediaPlay/Shader/vertex.glsl`（如果需要）
- 确保3D功能正常工作

**文件**：
- `videoDecoder/opengl/shaders/StereoVideoYCbCr.frag`
- `videoDecoder/opengl/shaders/StereoVideoYCbCr.vert`（如果存在）

### 3. UI布局修复（中优先级）
**目标布局**（根据图片）：
- 视频播放区域：左侧/中央（占据大部分空间）
- 播放列表面板：右侧（较窄的垂直面板）
- 控制条：底部（播放控制、时间显示、音量、3D功能）

**操作**：
- 修改`MainWindow.ui`
- 调整布局和控件位置
- 确保响应式布局

**文件**：
- `MainWindow.ui`
- `MainWindow.cpp`（如果需要调整代码）

### 4. DebugImageSaver集成（低优先级）
**功能**：
- 在TRACE模式下保存图像
- 保存位置：解码后、渲染前、渲染后

**操作**：
- 在VideoThread中保存解码后的帧
- 在VideoRenderer中保存渲染前后的帧
- 使用DebugImageSaver异步保存

**文件**：
- `videoDecoder/VideoThread.cpp`
- `videoDecoder/opengl/OpenGLRenderer.cpp`
- `videoDecoder/opengl/StereoOpenGLRenderer.cpp`

## 实施顺序

1. **Shader还原**（最快，影响颜色问题）
2. **硬件解码重构**（关键，影响黑画面问题）
3. **UI布局修复**（用户体验）
4. **DebugImageSaver集成**（调试支持）

## 注意事项

1. Shader还原后需要测试3D功能是否正常
2. 硬件解码重构需要参考QtAV但适配Windows D3D11VA
3. UI布局调整需要保持功能完整性
4. DebugImageSaver需要控制保存频率，避免性能影响
