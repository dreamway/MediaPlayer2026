# WZMediaPlayer 当前任务优先级

## 优先级说明

- 🔴 **P0 - 最高优先级**：核心功能修复，影响基本播放
- 🟡 **P1 - 高优先级**：重要功能，影响用户体验
- 🟢 **P2 - 中优先级**：功能增强，提升体验
- 🔵 **P3 - 低优先级**：架构优化，支线任务

---

## P0 - 核心功能修复（最高优先级）

### 1. 硬件解码修复 🔴
**状态**：部分完成
- ✅ 已修复 transferFrame 中的 sw_frame 初始化问题
- ✅ 已启用硬件解码
- ⚠️ **需要测试验证是否解决黑画面问题**

**下一步**：
- [ ] 测试硬件解码功能
- [ ] 如果仍有黑画面，继续调试
- [ ] 参考 QtAV 进一步优化

**相关文件**：
- `videoDecoder/hardware_decoder.cc`
- `videoDecoder/hardware_decoder.h`
- `videoDecoder/FFDecHW.cpp`

### 2. 基础播放功能修复 🔴
**问题**：
- 色彩问题（可能与 shader 相关）
- 无声音
- Seeking 后画面/音频不同步
- AV 同步问题

**下一步**：
- [ ] 还原 shader（使用 branches 中的 fragment.glsl）
- [ ] 调试音频输出问题
- [ ] 修复 Seeking 逻辑
- [ ] 优化 AV 同步

**相关文件**：
- `videoDecoder/opengl/shaders/StereoVideoYCbCr.frag`
- `PlayController.cpp` (Seeking, AV sync)
- `videoDecoder/AudioThread.cpp`
- `videoDecoder/VideoThread.cpp`

---

## P1 - 重要功能（高优先级）

### 3. 测试闭环完善 🟡
**状态**：部分完成
- ✅ 已创建进度条和 Seeking 测试
- ✅ 已创建音频测试
- ⚠️ **需要运行测试验证效果**

**下一步**：
- [ ] 运行新的测试模块
- [ ] 分析测试结果
- [ ] 修复测试发现的问题
- [ ] 完善测试覆盖（边界条件、极端情况）

**相关文件**：
- `testing/pywinauto/test_progress_seek.py`
- `testing/pywinauto/test_audio.py`
- `testing/pywinauto/run_all_tests.py`

---

## P2 - 功能增强（中优先级）

### 4. 阶段四：统计信息系统 🟢
**状态**：未开始

**目标**：添加独立的统计信息类，便于调试和性能分析

**实施步骤**：
- [ ] 创建 `Statistics` 类
- [ ] 集成到 `VideoThread`、`AudioThread`、`PlayController`、`VideoRenderer`
- [ ] 在 UI 中显示统计信息（可选）

**相关文件**：
- `videoDecoder/Statistics.h/cpp` (待创建)
- `videoDecoder/VideoThread.cpp` (待修改)
- `videoDecoder/AudioThread.cpp` (待修改)
- `PlayController.cpp` (待修改)

### 5. 阶段六：播放列表优化 🟢
**状态**：未开始

**目标**：优化播放列表管理和播放控制逻辑

**实施步骤**：
- [ ] 创建独立的 `Playlist` 类
- [ ] 创建 `PlaylistItem` 类
- [ ] 集成到 `PlayController`
- [ ] UI 重构（播放列表、播放控制按钮独立化）

**相关文件**：
- `playlist/Playlist.h/cpp` (待创建)
- `playlist/PlaylistItem.h/cpp` (待创建)
- `PlayController.h/cpp` (待修改)

---

## P3 - 架构优化（低优先级，支线任务）

### 6. Camera 架构改进 🔵
**状态**：部分完成
- ✅ `CameraManager` 已创建
- ✅ `MainWindow` 中已部分集成
- ⏸️ Camera Widget 同级化（待实施）

**说明**：这是支线任务，优先级低于核心功能修复。

**改进方案**：
- [ ] 将 `CameraOpenGLWidget` 提升为与 `StereoVideoWidget` 同级
- [ ] 在 `MainWindow` 中统一管理切换
- [ ] 移除 `StereoVideoWidget` 中的 Camera 接口

**相关文件**：
- `MainWindow.h/cpp`
- `StereoVideoWidget.h/cpp`
- `CameraManager.h/cpp`

**建议**：先完成 P0、P1、P2 任务，再考虑此改进。

### 7. UI 布局修复 🔵
**状态**：未开始

**目标**：按照图片调整 UI 布局

**说明**：优先级低，可以逐步实施。

**相关文件**：
- `MainWindow.ui`

### 8. DebugImageSaver 集成 🔵
**状态**：未开始

**功能**：在 TRACE 模式下保存图像

**说明**：调试支持功能，优先级低。

**相关文件**：
- `videoDecoder/opengl/DebugImageSaver.cpp`
- `videoDecoder/VideoThread.cpp`
- `videoDecoder/opengl/OpenGLRenderer.cpp`

---

## 实施顺序建议

### 立即执行（本周）
1. **测试硬件解码修复效果**（P0）
2. **还原 shader 文件**（P0）
3. **运行新的测试模块**（P1）
4. **修复测试发现的基础播放问题**（P0）

### 短期（1-2周）
1. **继续硬件解码调试**（如果仍有问题）（P0）
2. **修复色彩、音频、Seeking 等问题**（P0）
3. **完善测试闭环**（P1）

### 中期（2-4周）
1. **实施阶段四（统计信息系统）**（P2）
2. **实施阶段六（播放列表优化）**（P2）
3. **继续 BUG 修复和优化**（P0）

### 长期（1-2月）
1. **Camera 架构改进**（P3，支线任务）
2. **UI 布局修复**（P3）
3. **DebugImageSaver 集成**（P3）
4. **性能优化和稳定性测试**

---

## 注意事项

1. **Camera 重构是支线任务**：不要过度投入，先完成核心功能
2. **测试驱动**：修复问题后立即运行测试验证
3. **渐进式改进**：不要一次性改动太多，保持代码可编译可运行
4. **文档同步**：重要改动及时更新文档
